var people = ["Jon", "Jacob", "Jingle", "Heimer", "Schmidt"]
var alphabet = ["abcdefghijklmnopqrstuvwxyz"]
var newArr = people.concat(alphabet)
console.log(newArr)
for (var i = 0; i < newArr.length; i++)
for ( var j = 0; j < newArr.length[1]; j++){
    console.log(newArr[0],[1])
}